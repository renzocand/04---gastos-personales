# === Monorepo root ===
# Cada subproyecto (frontend/) tiene además su propio .gitignore.

# Backend (Spring Boot / Maven)
backend/target/
backend/.mvn/wrapper/maven-wrapper.jar
*.class

# Logs
*.log

# IDEs y editores
.idea/
*.iml
.project
.classpath
.settings/

# Variables de entorno (secretos). Se versiona solo .env.example.
.env

# Sistema
.DS_Store
Thumbs.db
