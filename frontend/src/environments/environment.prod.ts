// Entorno de producción. Por defecto asume que la API se sirve en el mismo
// origen bajo /api; ajustá apiUrl según dónde despliegues el backend.
export const environment = {
  production: true,
  apiUrl: '/api',
};
