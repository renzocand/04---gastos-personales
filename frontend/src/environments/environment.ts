// Entorno por defecto (desarrollo). En el build de producción Angular lo
// reemplaza por environment.prod.ts (ver fileReplacements en angular.json).
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api',
};
